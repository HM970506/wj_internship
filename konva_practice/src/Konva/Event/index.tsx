import { Layer, Circle } from "react-konva";
import ClickComponent from "./Click";

const EventIndex = () => {
  return (
    <Layer>
      <ClickComponent />
    </Layer>
  );
};

export default EventIndex;
